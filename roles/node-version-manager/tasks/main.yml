- name: install nvm
  git:
    repo: https://github.com/nvm-sh/nvm.git
    dest: '{{user_home}}/.nvm'
- name: setup .*rc nvm dir variables
  lineinfile:
    path: '{{item.dest}}'
    state: present
    line: '{{item.line}}'
  with_items:
    # zsh
    - { dest: '~/.zshrc', line: 'export NVM_DIR="$HOME/.nvm"' }
    - { dest: '~/.zshrc', line: '[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"' }
    - { dest: '~/.zshrc', line: '[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"' }
    # bash
    - { dest: '~/.bashrc', line: 'export NVM_DIR="$HOME/.nvm"' }
    - { dest: '~/.bashrc', line: '[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"' }
    - { dest: '~/.bashrc', line: '[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"' }
